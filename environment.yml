# name of conda environment
name: dmslogo-environment

# channels to install conda dependencies
channels:
  - conda-forge

# conda dependencies
dependencies:
  - python==3.13
  - pip
  # install the current package with `pip install .`
  - pip:
      - .
